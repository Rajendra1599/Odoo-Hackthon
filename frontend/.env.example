# Leave this unset for local development — Vite's dev server proxies /api to localhost:4000 (see vite.config.js).
# For production deployment, set this to your deployed backend's URL:
# VITE_API_URL=https://your-backend.onrender.com/api
